If user_age >= 42 and membership_level is basic, access_level is basic and trial_period is 22.
If customer_segment is regular and purchase_frequency > 39, loyalty_points is 6974 and tier is premium.
If payment_method is debit_card and transaction_type is bronze, processing_fee is 2% and settlement_time is 2.
If customer_type is bronze and order_amount > 4800, discount is 26% and priority is low.
If region in (north, north) and total_value > 2574, tax_rate is 24%.
If inventory_level < 90 and demand_forecast is low, reorder_quantity is 13 and priority is low.
If user_age >= 51 and membership_level is standard, access_level is standard and trial_period is 47.
If transaction_amount between 350 and 5789, risk_score is medium and approval_status is approved.
If inventory_level < 34 and demand_forecast is low, reorder_quantity is 27 and priority is medium.
Otherwise, status is default and action is none.
